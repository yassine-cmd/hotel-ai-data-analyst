[pytest]
testpaths = tests
# Keep Docker-dependent integration tests out of the default unit run.
# Run them explicitly with: pytest tests/integration -m integration
addopts = --ignore=tests/integration
asyncio_mode = auto
markers =
    integration: tests that require a running Docker daemon and the sandbox image
